extends ../layout
include ../mixins/work

block content
  each layerName in layers
    .layer.work
      iframe(src='/layer/render/' + current.hash + '/' + layerName, scrolling="no")
    p= layerName


